<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.a11810745.demomaps">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/iconee"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <uses-library
            android:name="org.apache.http.legacy"
            android:required="false" />

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyDZcWycXFUe-q_V8IO0z5F_l1gtSBzZa_E" />

        <activity
            android:name=".Inicial_Conv"
            android:label="@string/app_name"
            android:theme="@style/MeuEstilo1"></activity>
        <activity
            android:name=".InitialActivity"
            android:label="@string/app_name"
            android:theme="@style/MeuEstilo1">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Inicial_Anfi"
            android:label="@string/app_name"
            android:theme="@style/MeuEstilo1"></activity>
        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps"
            android:theme="@style/MeuEstilo"></activity>
        <activity
            android:name=".Filter"
            android:label=""
            android:theme="@style/MeuEstilo1"></activity>
        <activity
            android:name=".CadastroAnfitriao"
            android:label="@string/cadastro"
            android:theme="@style/MeuEstilo"></activity>
        <activity
            android:name=".CadastroConvidado"
            android:label="@string/cadastro"
            android:theme="@style/MeuEstilo"></activity>
        <activity
            android:name=".CadastroDeEndereco"
            android:label="@string/cadastro"
            android:theme="@style/MeuEstilo1"></activity>
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar"></activity>
        <activity
            android:name=".CadastroDeEventos"
            android:label="@string/app_name"></activity>
        <activity
            android:name=".CadastroLocalActivity"
            android:label="@string/title_activity_cadastro_local"></activity>
    </application>

</manifest>